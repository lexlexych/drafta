# Проверка реализации публикаций — 11 сентября 2026

Ветка: `codex/chatgpt-publication-drafts`, создана от `origin/main`.

## Выполнено

- Полный Vitest: **795 passed, 44 skipped** (100 успешных файлов, 6 пропущенных).
  Пропущенные существующие интеграционные тесты требуют настроенной БД.
- TypeScript: `tsc --noEmit` — успешно.
- ESLint: проверка всего репозитория.
- Production-сборка: `next build --webpack` — успешно; OAuth и API-маршруты динамические.
- Добавлены 23 unit/UI-теста: OAuth, границы файлов, категории и workspace-фильтры
  API, идемпотентность, блокировка ручного редактирования и сохранение изменений
  при Realtime. Обновлён тест регистрации Inngest-функций.
- Исправлены существующие ошибки типов в трёх тестовых файлах: сигнатуры mock-функций
  и отсутствующие поля фикстуры/props списка переписок. Продуктовая логика этих
  компонентов не менялась.

## Требует окружения и ручной настройки

- Миграция не применялась к удалённым БД.
- SQL/RLS-тест `supabase/tests/publication_drafts.test.sql` подготовлен, но не
  выполнен: локальный Docker daemon недоступен. Проверяет изоляцию, секретные
  таблицы, повторный импорт и блокировку перезаписи. Запуск: `supabase test db`
  после локальных миграций и seed проекта.
- Настоящий OAuth callback, получение знаний и передача файлов из текущего
  генератора ChatGPT требуют созданного вручную GPT, настроенных переменных
  окружения, dev-деплоя и работающего Inngest. Проверку с mock-запросами не считать
  подтверждением совместимости текущего генератора.
- Инструкция и сценарий приёмки:
  [custom-gpt-publications.md](custom-gpt-publications.md).
