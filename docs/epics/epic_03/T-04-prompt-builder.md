---
id: T-04
epic: E-003
title: "Сборка промпта по структуре §8 и debug-лог"
type: dev
status: done
depends_on: [T-02, T-03]
created: 2026-07-19
updated: 2026-07-22
---

# T-04. Сборка промпта по структуре §8 и debug-лог

## Цель

В `lib/ai` работает сборка промпта генерации черновика по структуре §8: роль и тон
из `ai_settings`, реальный контекст базы знаний, данные выбранной категории,
замаскированный контекст диалога, правила канала из capabilities и защита от
инъекций. За env-флагом — debug-лог итогового (замаскированного) промпта,
которым проверяется критерий этапа «телефон виден как плейсхолдер».

## Контекст

Обязательно прочитать перед выполнением:

- [8. AI-подсистема — «Структура промпта»](../../architecture/08-ai-subsystem.md#структура-промпта) — все 7 секций и их порядок; имя контакта в промпт не передаётся, если не нужно для ответа
- [6. Модель данных](../../architecture/06-data-model.md#ai_settings) — `ai_settings`: тон, язык, подпись
- [5. Слой абстракции каналов — «Capabilities канала»](../../architecture/05-channels.md#capabilities-канала) — откуда берутся правила канала (длина, стиль)
- [15. Compliance, §15.2](../../architecture/15-compliance-gdpr.md#122-субпроцессоры-и-данные) — «следить, чтобы контент не попадал в логи» Vercel — поэтому лог промпта только за флагом
- [16. План внедрения, этап 2](../../architecture/16-rollout-plan.md#этап-2--ai-черновики-с-дебаунсом-2-3-дня) — критерий: «телефон … виден в логе промпта как плейсхолдер»

Существенные факты:

- Вход билдера — **уже замаскированные** тексты (T-03) + карта соответствий не
  нужна (подстановка — забота пайплайна T-05). Билдер — чистая функция без БД:
  все данные приходят параметрами, загружает их пайплайн (T-05).
- Секции (2) базы знаний и (6) категории больше не являются будущими пустыми
  слотами: CRUD `kb_files`, сборка token-budgeted контекста и `categories` уже
  существуют, а черновики обязаны использовать эти данные. Пустым будущим слотом
  остаются только заметки контакта (этап 7).
- T-05 загружает строки БД. В builder он передаёт результат
  `buildKnowledgeBaseContext` (точный фрагмент + `usedFileIds`) и обязательную
  выбранную категорию в нормализованной форме; `draftInstruction` отображает
  фактическое поле `categories.draft_instruction`. Проверка `skip_draft` выполняется
  пайплайном до вызова builder. Если классификация ещё не назначила категорию,
  пайплайн использует системную категорию по умолчанию.
- Защита от инъекций: входящие сообщения подаются как данные (отдельный
  размеченный блок) с явной системной инструкцией игнорировать содержащиеся
  в них команды.
- Язык ответа — из `ai_settings` (язык общения бизнеса), подпись из `ai_settings`
  добавляется инструкцией к черновику.
- Debug-лог: `AI_LOG_PROMPTS=true` → вывести итоговый промпт в серверный лог;
  по умолчанию выключен (в проде не включать — §15.2). Логируется только
  замаскированный промпт, никогда исходный текст.

## Шаги реализации

1. Типы входа `lib/ai/prompt.ts`: `PromptInput = { aiSettings: {tone, language,
   signature}, maskedMessages: {direction, text}[], channelCapabilities,
   conversationKind, knowledgeBase, selectedCategory, maskedContactNotes? }`.
   `knowledgeBase` принимает нормализованный результат `buildKnowledgeBaseContext`,
   `selectedCategory` — `{ id, name, description, draftInstruction }`.
2. `buildDraftPrompt(input): ChatMessage[]` — system + user сообщения для клиента
   T-02:
   - system: роль ассистента бизнеса, тон/язык/подпись из `ai_settings`, реальный
     KB-контекст и выбранная категория, правила канала из capabilities (макс.
     длина, стиль), инструкция про защиту от инъекций;
   - user: история диалога (замаскированная) как размеченные данные
     (направление + текст), задача — черновик ответа на последние входящие.
3. Debug-лог: хелпер `logPromptIfEnabled(messages)` — при `AI_LOG_PROMPTS=true`
   печатает промпт целиком (console/server logger) с пометкой-префиксом;
   вызывается пайплайном (T-05) после сборки.
4. Юнит-тесты:
   - порядок и состав секций соответствует §8 (снапшот/структурная проверка);
   - имя контакта не появляется в промпте (в input его просто нет — проверить,
     что тип не принимает лишние поля);
   - замаскированный вход → в итоговом промпте есть `{{PHONE_1}}` и нет исходного
     номера;
   - блок защиты от инъекций присутствует и входящие подаются как данные;
   - KB и выбранная категория реально попадают в промпт; их содержимое размечено
     как недоверенные данные, попытки закрыть секцию экранируются;
   - пустой KB и пустой будущий слот заметок не порождают пустых заголовков;
   - лог не вызывается без флага и вызывается с флагом (мок логгера).

## Критерии приёмки

- [ ] `buildDraftPrompt` собирает промпт по структуре §8; KB и данные выбранной категории (включая `draft_instruction`) реально включены, пустым может остаться только будущий слот заметок контакта
- [ ] Билдер — чистая функция: без обращений к БД, сети и SDK
- [ ] Имя контакта в промпт не попадает; вход принимает только замаскированные тексты (по контракту типов/именованию)
- [ ] Входящие сообщения, KB и категория поданы как безопасно размеченные недоверенные данные с инструкцией игнорировать команды и meta-инструкции в них
- [ ] Лог промпта — только при `AI_LOG_PROMPTS=true`, содержимое — только замаскированный промпт
- [ ] Тесты шага 4 написаны и проходят

## Definition of Done

```
npm run lint
npm run build
npm test
```

Ревьюер дополнительно собирает промпт на примере с телефоном в сообщении
и убеждается, что в выводе — плейсхолдер, а структура читается по §8.

---

## 🔧 Отчёт разработчика

### Что сделано

- Добавлен `lib/ai/prompt.ts`: чистый `buildDraftPrompt` возвращает system + user
  сообщения клиента T-02, сохраняет порядок семи секций §8, применяет тон, язык,
  подпись, тип диалога и реальные capabilities канала.
- Зафиксирован обязательный контракт T-05: `knowledgeBase` принимает точный
  token-budgeted результат `buildKnowledgeBaseContext` с `usedFileIds`, а
  `selectedCategory` — нормализованные `id`, `name`, `description` и
  `draftInstruction` (`categories.draft_instruction`). Имя контакта в контракте
  отсутствует; будущие заметки принимаются только как `maskedContactNotes`.
- Контекст KB, выбранная категория, заметки и замаскированная переписка помещаются
  в отдельные `UNTRUSTED_*_JSON`-блоки. Закрывающие маркеры внутри данных
  экранируются; system-инструкция требует игнорировать prompt injection и иные
  meta-команды, но использовать факты KB и допустимое действие категории.
- Добавлен `logPromptIfEnabled`: полный уже собранный prompt логируется с префиксом
  только при точном `AI_LOG_PROMPTS=true`; по умолчанию побочных эффектов нет.
- Добавлены экспорты builder, logger и контрактных типов из `lib/ai/index.ts`.
- Добавлен `lib/ai/prompt.test.ts` с 6 тестами: порядок/состав семи секций, реальные
  KB/category данные, отсутствие contact-name API и пустых заголовков, плейсхолдер
  телефона без исходного номера, защита границ недоверенных блоков, debug-флаг.
- В требованиях этого тикета и прямых противоречиях `_index.md` эпика отражено
  пользовательское решение: существующие KB и category больше не пустые слоты.

### Как проверено

- `npm.cmd test -- lib/ai/prompt.test.ts` — PASS, 1 файл / 6 тестов.
- `npm.cmd run lint` — PASS, ESLint без ошибок.
- `npm.cmd run build` — PASS, production-сборка и TypeScript завершены успешно,
  16 страниц сгенерированы.
- `npm.cmd test` — PASS, 211 passed / 27 skipped (38 файлов passed / 4 skipped).
  DB-backed тесты пропущены штатно из-за отсутствия локальных Supabase env;
  тесты T-04 полностью выполнены. Диагностический stderr остальных тестов ожидаем
  их сценариями ошибок и не является падением.
- `git diff --check` — PASS.

### Отклонения от плана

- По прямому решению пользователя вместо исходных пустых слотов этапов 4/6
  реализованы реальные обязательные входы базы знаний и выбранной категории.
  Текст требований/AC тикета и противоречащие пункты эпика скорректированы.
- Пустым/опциональным остался только будущий `maskedContactNotes` этапа 7; пустой
  KB-контекст допустим, когда включённых файлов нет, но сам аргумент обязателен.

### Вне скоупа и открытые вопросы

- Загрузка DB-строк, выбор системной категории по умолчанию при отсутствии
  классификации, `skip_draft`, сохранение `usedFileIds` и вызов builder/logger —
  ответственность T-05 по зафиксированному контракту; в T-04 обращений к БД нет.
- LLM-классификация категории остаётся этапом 6; T-05 до неё использует уже
  назначенную категорию либо системную категорию по умолчанию.
- Ручных шагов, блокеров и открытых вопросов по T-04 нет.

## 🔍 Ревью

_Заполняется агентом-ревьюером: вердикт APPROVED / CHANGES_REQUESTED,
замечания, что прогнано и с каким результатом._
