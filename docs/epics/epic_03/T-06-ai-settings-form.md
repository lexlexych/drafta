---
id: T-06
epic: E-003
title: "Настройки → AI: форма реальных ai_settings"
type: dev
status: todo
depends_on: []
created: 2026-07-19
updated: 2026-07-19
---

# T-06. Настройки → AI: форма реальных ai_settings

## Цель

Раздел «Настройки → AI» показывает и сохраняет реальные значения `ai_settings`
workspace: тон, язык, подпись, период дебаунса, авто-генерация (отдельно для DM и
комментариев), модель. Mock-форма из UI-каркаса E-001/T-07 заменяется на рабочую;
сохранённый `debounce_seconds` реально влияет на дебаунс `generate-draft`.

## Контекст

Обязательно прочитать перед выполнением:

- [6. Модель данных](../../architecture/06-data-model.md#ai_settings) — состав `ai_settings`; `debounce_seconds` по умолчанию ~45–60
- [10. UI и навигация — «Разделы настроек»](../../architecture/10-ui.md#разделы-настроек) — раздел «AI»
- [2. Глоссарий](../../architecture/02-glossary.md) — термины в именах типов/компонентов
- [12. Структура репозитория](../../architecture/12-repo-structure.md) — `app/(app)/settings/`

Существенные факты:

- Строка `ai_settings` с дефолтами создаётся при онбординге workspace
  (E-001/T-05, RPC `create_workspace`) — форма всегда редактирует существующую
  строку; на всякий случай сохранение делает upsert.
- RLS-политики E-001/T-03 разрешают участнику workspace SELECT/UPDATE — работать
  через пользовательскую сессию (publishable-клиент/server actions), серверный
  ключ здесь не нужен.
- UI-каркас раздела уже свёрстан (E-001/T-07, mock-форма-заглушка) — не верстать
  с нуля, а подключить к данным; состав полей выровнять по §6.
- Поле «модель»: селект из короткого списка, определённого конфигом `lib/ai`
  (T-02), плюс дефолт «авто» (пусто = дефолт конфига). Точный список моделей —
  открытый вопрос №2 эпика; заложить конфигурируемый список, не хардкод в
  компоненте.
- Схему таблицы не менять; если состава колонок не хватает под §6 — это ошибка
  E-001, остановиться и зафиксировать вопрос, а не мигрировать молча.

## Шаги реализации

1. Server-загрузка `ai_settings` текущего workspace в страницу раздела
   (`app/(app)/settings/…`).
2. Server action сохранения: валидация (тон/язык/подпись — строки с лимитами;
   `debounce_seconds` — целое, диапазон 0–600; флаги авто-генерации — boolean;
   модель — из списка конфига или пусто) → upsert по workspace.
3. Подключить существующую форму каркаса: поля из §6, состояния «сохранено/ошибка»,
   мобильная компоновка не ломается.
4. Тумблеры авто-генерации: DM и комментарии раздельно (комментарии — с пометкой,
   что черновики для комментариев появятся на этапе 5; тумблер сохраняется уже
   сейчас).
5. Тесты: юнит на валидацию action (граничные значения `debounce_seconds`,
   неизвестная модель отклоняется); смоук-тест рендера раздела; негативный
   тест RLS — пользователь другого workspace не может обновить чужие
   `ai_settings` (по образцу RLS-тестов E-001/T-06).

## Критерии приёмки

- [ ] Раздел показывает актуальные значения `ai_settings` workspace (не mock)
- [ ] Сохранение персистит все поля §6: тон, язык, подпись, `debounce_seconds`, авто-генерация DM/комментарии, модель
- [ ] Валидация отклоняет некорректные значения (диапазон дебаунса, чужая модель) с понятной ошибкой в UI
- [ ] Работа через пользовательскую сессию под RLS; чужой workspace недоступен (негативный тест)
- [ ] Изменённый `debounce_seconds` подхватывается `generate-draft` (ручная проверка совместно с T-05, если T-05 уже done; иначе — проверка значения в БД)
- [ ] Термины глоссария соблюдены; mock-данные раздела удалены из mock-модуля или помечены неиспользуемыми

## Definition of Done

```
npm run lint
npm run build
npm test
```

Плюс ручная проверка в браузере: изменить значения, перезагрузить страницу —
значения сохранились; на мобильной ширине форма пригодна.

---

## 🔧 Отчёт разработчика

_Заполняется агентом-разработчиком: что сделано (файлы), как проверено
(команды и результат), отклонения, «вне скоупа», вопросы._

## 🔍 Ревью

_Заполняется агентом-ревьюером: вердикт APPROVED / CHANGES_REQUESTED,
замечания, что прогнано и с каким результатом._
