---
id: T-02
epic: E-003
title: "Клиент LLM в lib/ai: Mistral с fallback на OpenRouter"
type: dev
status: done
depends_on: []
created: 2026-07-19
updated: 2026-07-21
---

# T-02. Клиент LLM в lib/ai: Mistral с fallback на OpenRouter

## Цель

В `lib/ai` работает единая точка вызова LLM: OpenAI-совместимый SDK со сменным
`baseURL`, провайдер выбирается по env-переменным без изменения кода — задан
`MISTRAL_API_KEY` → Mistral (La Plateforme), иначе → OpenRouter
(`OPENROUTER_API_KEY` + `OPENROUTER_MODEL`). Выбор провайдера покрыт юнит-тестами;
прямых обращений к SDK за пределами `lib/ai` нет.

## Контекст

Обязательно прочитать перед выполнением:

- [8. AI-подсистема — «Клиент и выбор провайдера» и «Резервный провайдер»](../../architecture/08-ai-subsystem.md#клиент-и-выбор-провайдера) — логика выбора дословно; активен всегда ровно один провайдер
- [13. Окружения, секреты](../../architecture/13-environments-secrets.md#секреты-vercel-env) — `MISTRAL_API_KEY`, `OPENROUTER_API_KEY`, `OPENROUTER_MODEL`
- [12. Структура репозитория](../../architecture/12-repo-structure.md) — `lib/ai/` и граница «вызовы LLM только через lib/ai»
- [14. Правила вайбкодинга](../../architecture/14-vibecoding-rules.md) — правило 9

Существенные факты:

- Клиент — стандартный OpenAI-совместимый SDK (npm-пакет `openai`) с `baseURL`
  провайдера; отдельные SDK Mistral/OpenRouter не подключать.
- Модель по умолчанию для Mistral в архитектуре не зафиксирована (открытый вопрос
  №2 эпика): завести конфигурируемую константу в `lib/ai/config.ts`
  (предложение — `mistral-small-latest`) с возможностью переопределения параметром
  вызова (источником станет `ai_settings.model`, потребитель — T-05).
- Ретраи здесь не реализуются — ими управляют шаги Inngest (T-05); клиент лишь
  бросает нормализованную ошибку и задаёт разумный таймаут запроса.
- Ключи API не должны попадать в логи и в клиентский бандл: модуль — только
  серверный (`server-only`).

## Шаги реализации

1. `lib/ai/config.ts`: `resolveProvider()` → `{ provider: "mistral" | "openrouter",
   baseURL, apiKey, defaultModel }`:
   - `MISTRAL_API_KEY` задан → Mistral: `baseURL` La Plateforme, дефолтная модель
     из константы;
   - иначе `OPENROUTER_API_KEY` задан → OpenRouter: `baseURL`
     `https://openrouter.ai/api/v1`, модель — `OPENROUTER_MODEL` (не задана вместе
     с ключом → понятная ошибка конфигурации);
   - не задан ни один → понятная ошибка при первом вызове (не при импорте модуля —
     сборка без ключей должна проходить).
2. `lib/ai/client.ts` (`import "server-only"`): функция вида
   `generateCompletion(messages, { model?, temperature?, maxTokens? })` →
   текст ответа; таймаут запроса; нормализованная ошибка (`AiProviderError`
   с кодом/статусом, без утечки ключей в message).
3. Индекс `lib/ai/index.ts` — публичная поверхность модуля (клиент; позже —
   маскирование T-03 и промпт T-04).
4. Юнит-тесты (env подменяется в тестах):
   - оба ключа заданы → Mistral (приоритет);
   - только `OPENROUTER_API_KEY` + `OPENROUTER_MODEL` → OpenRouter с этой моделью;
   - `OPENROUTER_API_KEY` без `OPENROUTER_MODEL` → ошибка конфигурации;
   - ни одного ключа → ошибка при вызове, а не при импорте;
   - переданный параметр `model` переопределяет дефолт.
   Сетевые вызовы в тестах мокаются — живой LLM не дёргать.

## Критерии приёмки

- [ ] Провайдер выбирается строго по логике §8: `MISTRAL_API_KEY` → Mistral, иначе OpenRouter; смешивания нет
- [ ] Один клиент/SDK, различие провайдеров — только `baseURL`, ключ и модель (конфигурация, не код)
- [ ] Модуль серверный (`server-only`); ключи не появляются в логах и сообщениях ошибок
- [ ] За пределами `lib/ai` нет импортов LLM SDK (проверка grep — правило 9)
- [ ] Юнит-тесты из шага 4 написаны и проходят
- [ ] Сборка проходит без задания LLM-ключей

## Definition of Done

```
npm run lint
npm run build
npm test
```

Ревьюер дополнительно проверяет grep'ом отсутствие импортов `openai`
вне `lib/ai` и отсутствие ключей в тексте ошибок/логов.

---

## 🔧 Отчёт разработчика

### Что сделано

- Добавлен `lib/ai/config.ts`: отложенный до первого вызова выбор ровно одного
  провайдера, приоритет Mistral при наличии `MISTRAL_API_KEY`, OpenRouter только
  при отсутствии ключа Mistral, проверка обязательного `OPENROUTER_MODEL`.
- Добавлен серверный `lib/ai/client.ts`: единая функция `generateCompletion`,
  OpenAI-совместимый SDK с конфигурируемыми `baseURL`/ключом/моделью, таймаутом
  30 секунд и отключёнными SDK-ретраями. Ошибки нормализуются в
  `AiProviderError` без сообщения upstream и без утечки ключей. Ошибка Mistral
  не вызывает переключение на OpenRouter.
- Добавлен публичный серверный вход `lib/ai/index.ts`.
- В `package.json` и `package-lock.json` добавлен единый SDK `openai`.
- Добавлены `lib/ai/config.test.ts` и `lib/ai/client.test.ts`: покрыты все
  сценарии выбора провайдера из тикета, отложенная ошибка конфигурации,
  переопределение модели, параметры клиента, отсутствие runtime-fallback после
  ошибки Mistral и отсутствие ключа в нормализованном сообщении.

### Проверки

- `npm run lint` — успешно, ошибок ESLint нет.
- `$env:MISTRAL_API_KEY=''; $env:OPENROUTER_API_KEY='';
  $env:OPENROUTER_MODEL=''; npm run build` — успешно; production build и
  TypeScript-проверка прошли без LLM-ключей, сгенерированы 16 статических страниц.
- `npm test` — успешно: 36 файлов / 189 тестов прошли; 4 файла / 27 DB-тестов
  штатно пропущены без локального Supabase. На промежуточном прогоне один раз
  нестабильно упал чужой UI-тест базы знаний; изолированный повтор и финальный
  полный прогон прошли.
- `rg -n -i "openai" --glob "*.ts" --glob "*.tsx"
  --glob "!lib/ai/**" --glob "!node_modules/**" .` — совпадений нет: SDK не
  импортируется за пределами `lib/ai`.
- `git diff --check` — успешно.

### Отклонения

- Название тикета использует слово fallback, но согласно архитектуре и
  пользовательскому уточнению это только fallback конфигурации окружения:
  OpenRouter выбирается исключительно при отсутствии `MISTRAL_API_KEY`.
  Автоматического переключения после таймаута, 429 или 5xx нет.

### Вне скоупа и открытые вопросы

- База знаний и категории к вызову клиента не подключались: это скоуп следующих
  тикетов T-04/T-05.
- Ручных шагов и открытых вопросов для этого тикета не обнаружено.

## 🔍 Ревью

**Вердикт: APPROVED.**

Критерии приёмки и Definition of Done перепроверены независимо. При
одновременно заданных ключах выбирается Mistral. После ошибки Mistral
клиент нормализует и пробрасывает ошибку; ветки runtime-fallback на OpenRouter
нет, SDK-ретраи отключены (`maxRetries: 0`). OpenRouter выбирается только
при отсутствии `MISTRAL_API_KEY`. Клиент защищён `server-only`; upstream message
не попадает в `AiProviderError`, логирования секретов нет. Импортов SDK
`openai` вне `lib/ai` и провайдерных SDK нет; в клиентских build-ассетах
LLM-ключей и URL провайдеров нет. Правила вайбкодинга, затронутые
тикетом, соблюдены; лишних изменений в commit тикета нет.

Прогоны:

- `npm.cmd test -- --run lib/ai/config.test.ts lib/ai/client.test.ts` — 2 файла,
  8 тестов прошли;
- `npm.cmd run lint` — успешно;
- `npm.cmd test` — 36 файлов / 189 тестов прошли, 4 DB-файла /
  27 тестов пропущены без локального Supabase;
- при явно пустых `MISTRAL_API_KEY`, `OPENROUTER_API_KEY`,
  `OPENROUTER_MODEL`: `npm.cmd run build` — успешно, 16 статических
  страниц;
- grep/`rg` импортов `openai` вне `lib/ai`, провайдерных SDK,
  секретов/логов и LLM-маркеров в `.next/static` — запрещённых
  совпадений нет;
- `git diff --check` для commit тикета и текущего worktree — успешно.
