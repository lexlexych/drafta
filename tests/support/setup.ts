/**
 * Общий setup для всего набора тестов.
 *
 * У `waitFor` и `findBy*` из Testing Library таймаут по умолчанию — одна
 * секунда. В одиночном прогоне этого с запасом хватает: React-переход оседает
 * за десятки миллисекунд. Но на полном прогоне воркеры делят машину, и та же
 * секунда перестаёт быть запасом — тест не находит кнопку, которая появится
 * мгновением позже, и набор краснеет случайным образом, без всякой связи с
 * изменённым кодом.
 *
 * Пять секунд настоящую поломку не прячут: сломанный код не зеленеет от
 * ожидания, он падает на несколько секунд позже. Прячется ровно то, что и
 * должно, — разница в скорости машины.
 *
 * Проверка на `document` нужна, потому что setup выполняется и для тестов в
 * окружении `node`, где DOM-у взяться неоткуда.
 */

import { configure } from "@testing-library/dom";

if (typeof document !== "undefined") {
  configure({ asyncUtilTimeout: 5000 });
}
